import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';
import 'bootstrap/dist/css/bootstrap.min.css';

// Dito ko na nirender yung <App /> component sa loob ng React.StrictMode.
// Ang StrictMode ay tumutulong mag-detect ng possible issues sa code habang nagde-develop.
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

reportWebVitals();
